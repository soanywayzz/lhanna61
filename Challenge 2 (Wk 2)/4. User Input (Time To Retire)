# Ask the user to input their age and tell them when they can retire (age 65)
age = int(input('what is your age? '))
if age < 65:
    ret_age = 65 - age
    print ('you can retire in {0} years'.format(ret_age))

else:
    print('you are retired')

# OUTPUT = what is your age?
# "28" (typed in)
# you can retire in 37 years
